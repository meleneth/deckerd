= form_for @magic_card_type do |f|
  - if @magic_card_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@magic_card_type.errors.count, "error")} prohibited this magic_card_type from being saved:"
      %ul
        - @magic_card_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
